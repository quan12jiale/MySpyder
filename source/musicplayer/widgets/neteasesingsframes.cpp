#include "neteasesingsframes.h"
#include "music.h"

NetEaseSingsArea::NetEaseSingsArea(MainContent *parent)
    : SingsFrameBase (parent)
{
    this->config = nullptr;
}

NetEaseSearchResultFrame::NetEaseSearchResultFrame(QWidget* parent)
    : SingsSearchResultFrameBase (parent)
{

}
